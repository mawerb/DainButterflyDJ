import { z } from 'zod';
import { DainResponse } from '../../DainResponse';
export interface OAuth2ToolDependencies {
    createTool: <TInput, TOutput>(config: {
        id: string;
        name: string;
        description: string;
        input: z.ZodType<TInput>;
        output: z.ZodType<TOutput>;
        pricing: {
            pricePerUse: number;
            currency: string;
        };
        handler: (input: TInput, agentInfo: any, context: {
            app: any;
        }) => Promise<DainResponse>;
    }) => any;
}
export declare function createOAuth2Tool(provider: string, prefix: string | undefined, deps?: OAuth2ToolDependencies): any;
